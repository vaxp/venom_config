# كيفية إضافة خصائص جديدة لنظام Venom

هذا الدليل يشرح خطوة بخطوة كيف تضيف تحكماً جديداً للنظام (مثلاً: تريد التحكم في **لون الأزرار** في تطبيق الآلة الحاسبة).

العملية تتكون من 3 خطوات بسيطة: **التسمية**، **العميل**، و **الإعدادات**.

---

## 1️⃣ الخطوة الأولى: اختر "مفتاحاً" (Key)
نظام Venom يعتمد على المفاتيح النصية. لكي يبقى النظام نظيفاً، نتبع هذه التسمية:

`category.app_name.property`

**أمثلة:**
*   `apps.calculator.button_color` (لون أزرار الآلة الحاسبة)
*   `system.font_family` (نوع الخط للنظام كله)
*   `dock.icon_size` (حجم الأيقونات في الشريط السفلي)

> **نصيحة:** اختر اسماً واضحاً ومفهوماً باللغة الإنجليزية.

---

## 2️⃣ الخطوة الثانية: جهز العميل (Client Side)
اذهب للتطبيق الذي تريد أن يتغير (مثلاً: الآلة الحاسبة).

1.  عرف متغيراً لتخزين القيمة (مع قيمة افتراضية).
2.  اقرأ المفتاح الجديد داخل `_loadConfig`.

```dart
class CalculatorButton extends StatefulWidget {
  @override
  _CalculatorButtonState createState() => _CalculatorButtonState();
}

class _CalculatorButtonState extends State<CalculatorButton> {
  // 1. القيمة الافتراضية (مثلاً أزرق)
  Color _btnColor = Colors.blue; 

  @override
  void initState() {
    super.initState();
    // الاشتراك في التغييرات
    _loadConfig(VenomConfig().getAll());
    VenomConfig().onConfigChanged.listen((config) => _loadConfig(config));
  }

  void _loadConfig(Map<String, dynamic> config) {
    // 2. قراءة المفتاح الجديد الذي اخترته
    final hexColor = config['apps.calculator.button_color'];
    
    if (hexColor != null && mounted) {
      setState(() {
        _btnColor = _parseColor(hexColor);
      });
    }
  }

  @override
  Widget build(BuildContext context) {
    return Container(
      color: _btnColor, // استخدام المتغير
      child: Text("Click Me"),
    );
  }
}
```

---

## 3️⃣ الخطوة الثالثة: أضف التحكم في الإعدادات (Settings Side)
الآن اذهب لتطبيق الإعدادات (`venom_settings`) وأضف زراً أو واجهة للتحكم في هذا المفتاح.

كل ما عليك فعله هو استدعاء `VenomConfig().set` مع المفتاح الجديد.

```dart
// زر يغير لون أزرار الآلة الحاسبة للأحمر
ElevatedButton(
  onPressed: () async {
    // هنا نرسل القيمة للمفتاح الجديد
    await VenomConfig().set('apps.calculator.button_color', '#FF0000');
  },
  child: Text("Make Calculator Red"),
)
```

---

## 💡 ملخص
لإضافة أي ميزة جديدة مستقبلاً:
1.  **اخترع اسماً** للميزة (Key).
2.  اجعل تطبيقك **يستمع** لهذا الاسم ويغير شكله بناءً عليه.
3.  اجعل تطبيق الإعدادات **يرسل** قيماً لهذا الاسم.

لا تحتاج لتعديل مكتبة `venom_config` نفسها أبداً! هي مجرد ساعي بريد ينقل رسائلك. 📬
